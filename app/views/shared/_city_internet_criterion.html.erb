<div class="criterion<%= " best" if defined?(best) && best == item %>">
  <div class="mr-3">
    <i class="fas fa-wifi"></i>
  </div>
  <p class="mb-0">
    <span class="criterion-number"><%= item.ftth_homes_rate.round %> %</span><br>
    <span class="text-muted" style="font-size: 0.9rem;">Logements fibrés</span>
  </p>
</div>
